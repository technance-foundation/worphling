{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "pnpm -r build",
        "prepare": "pnpm run build",
        "playground": "pnpm --filter @invexa/worphling-playground start"
    }
}
